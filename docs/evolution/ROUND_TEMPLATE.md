# 回合记录模板

> 复制本节内容到 `ITERATION_LOG.md` 末尾，替换 `<...>` 占位符。

---

## R<N> — <回合主题，一句话>

- **回合序号**：<N> / 50+
- **所属阶段**：A|B|C|D|E
- **开始时间**：<YYYY-MM-DD>
- **状态**：⏳ 进行中 | ✅ 完成 | ⚠️ 部分完成（续 R<N+1>）

### 本轮目标

<3–6 条具体、可验证的目标。每回合聚焦一个主题，避免贪多。>

### 设计决策

<关键取舍：为什么这么做？参考了 Grok 的哪个 crate / 理念？与现有架构如何兼容？>

### 实现 / 产出

<新建/修改的文件清单 + 简要说明。契约变更须同步三处：`ipc-contract.md`、`ipc.ts`、`protocol.py`。>

### 验证

<跑了的测试 / smoke / 类型检查。用数据说话：通过 N 个测试、覆盖率、性能数字。失败必须如实记录。>

### Commit

`<type>(<scope>): R<N> <一句话摘要>`
