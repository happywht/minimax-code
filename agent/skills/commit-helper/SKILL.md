---
name: commit-helper
description: |
  Helps craft well-formed git commit messages following Conventional
  Commits. Invoked when the user asks the agent to "commit this" or
  to suggest a commit message based on the current diff.

  This is a stub for the skeleton milestone — the loader/registry
  lands in the `skills-system` task.
---

# Commit Helper (stub)

This skill exists so the skill loader has at least one entry on disk
to discover. The real implementation arrives with the `skills-system`
task.
